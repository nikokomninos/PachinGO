# Docker Compose reads this file automatically after you copy it to .env.
# Do not commit the copied file.
TUNNEL_TOKEN=
# Optional: use an absolute private environment-file path on the server.
BACKEND_ENV_FILE=./backend/.env
